    <% if @entry.errors.any? %>
      <div class="ui negative message">
      <i class="close icon"></i>
        <% @entry.errors.full_messages.each do |msg| %>
          <p><%= msg %></p>
        <% end %>
      </div>
    <% end %>
    
<%= form_for(@entry, html: {class: 'ui form'}) do |f| %>
<div class="ui raised segment">

    <div class="field">
      <label>タイトル</label>
      <%= f.text_field :title, class: "" %>
    </div>
    
    <div class="field">
      <label>記事のURL</label>
      <%= f.text_field :url, class: "" %>
    </div>
    
    <div class="field">
      <label>作者</label>
      <%= f.text_area :author, class: "" %>
    </div>
    
    <div class="field">
      <label>カテゴリ</label>
      <%= f.text_field :category %>
    </div>
  
  <div class="actions">
    <%= f.submit "登録", class: 'ui button'%>
  </div>
</div>
<% end %>